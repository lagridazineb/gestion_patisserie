# En local, laisse cette ligne commentée : le client utilisera http://localhost:3001/api par défaut.
# En production (Render Static Site), ajoute cette variable dans Settings > Environment
# avec l'URL réelle de ton backend (celui qui tourne en Web Service) :
# VITE_API_URL=https://dianna1.onrender.com/api
